// aboutblank.onFirefoxLoad = function(event) {
  // document.getElementById("contentAreaContextMenu")
          // .addEventListener("popupshowing", function (e){ aboutblank.showFirefoxContextMenu(e); }, false);
// };

// aboutblank.showFirefoxContextMenu = function(event) {
  // show or hide the menuitem based on what the context menu is on
  // document.getElementById("context-aboutblank").hidden = gContextMenu.onImage;
// };

// window.addEventListener("load", function () { aboutblank.onFirefoxLoad(); }, false);
